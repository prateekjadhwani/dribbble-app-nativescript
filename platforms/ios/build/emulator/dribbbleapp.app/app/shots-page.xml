<Page loaded="pageLoaded">
	<ListView id="myListView" items="{{ myItems }}">
		<ListView.itemTemplate>
			<StackLayout orientation="vertical">
				<Image src="{{ imageSrc }}" stretch="none" />
				<Label text="{{ title }}" cssClass="message"/>
			</StackLayout>
		</ListView.itemTemplate>
	</ListView>
</Page>